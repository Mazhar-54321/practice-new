# Takeaway — Day 002: Type Annotations & Type Inference

- **Always use lowercase `string`, `number`, `boolean` in TypeScript — never `String`, `Number`, `Boolean`**

- **`any` = TypeScript switched off for that variable — avoid it always**

- **Union type = value can be one of multiple types. Optional = property can be absent entirely — two different things**
